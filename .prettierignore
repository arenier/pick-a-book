# Prettier ne touche pas au Markdown : la doc et les ADR sont enveloppes a la main
# a 100 colonnes, et un reformatage automatique brouillerait leurs diffs.
*.md

# Contenu gere hors du workspace
/.claude
/docs

/dist
**/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
.yarn
yarn.lock
